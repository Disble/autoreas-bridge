# Learning Log (Vitácora)

A human-readable record of *why* things in this repo are the way they are:
decisions taken and non-obvious problems solved, so future sessions inherit the
reasoning instead of rediscovering it.

This is a memory aid, **not** a substitute for deterministic guards. Whenever a
lesson can be enforced (linter, test, `go vet`, pre-commit gate), do that too —
this file only explains the *why*, it never replaces the *how*.

## How to append

- One lesson per line, kept to a single short sentence.
- Format: `- [YYYY-MM-DD]: text`
- Newest entries at the bottom. Never rewrite past entries; add a new line if
  something changes.

## Entries

- [2026-07-16]: Editor read DTOs (`AnimeEditorNullable*DTO`) must never use JSON `omitempty` on their value field — the `kind` discriminator already encodes missing/null, and `omitempty` silently drops a legit zero (tipo=0 "Anime (TV)"), which emptied the Type field and forced an endless no_op save loop.
- [2026-07-16]: Go↔JS zero-value/`omitempty` bugs pass per-side unit tests because each side is correct in isolation; catch this class with a contract test at the serialization boundary, not with more unit tests per layer.
- [2026-07-16]: Reply in the language the user actually typed this turn; do not default to Spanish from project config or stored memory when the user is writing in English.
- [2026-07-16]: In the Anime Editor, `props.initialAnimeId` is test-only scaffolding — the route mounts the workspace with no props, so production deep-link selection flows through `useParams().id` in `use-anime-editor-transitions.ts`, not the prop.
- [2026-07-16]: The editor rail's filter default is route-derived: a fresh `/editor/:id` mount opens on "All anime" so a non-watching deep-linked anime (e.g. "No me gusto") is visible in the rail, while the generic `/editor` entry keeps "Watching first"; a lazy `useState` initializer reads the param once so in-app clicks never reset the user's toggle.
- [2026-07-18]: A scheduled download run must never launch JDownloader just to check its status — `EnsureOnline(..., true)` auto-launches the exe, so it has to be deferred behind a lazy, concurrency-safe `jdGate` and resolved only inside `downloadAvailableEpisodes`, the earliest point that has actually proven at least one episode is missing.
- [2026-07-16]: When the Fallow changed-code gate blocks a commit on duplication/complexity, prefer the real fix — extract a generic `shared/ui` component (e.g. `LabeledTextField` collapsing repeated Label/Input/Description rows) and pull dense inline logic into a pure helper — over suppressing; reserve `fallow-ignore`/baseline for genuine false positives like structurally-similar but semantically-distinct interface shapes across bounded contexts.
- [2026-07-16]: Schedule apply needs two normalizations with different purposes: changed-record payloads must preserve board-wide destination order numbers for touched anime, while backend validation may normalize untouched legacy sparse weekday authority only after keeping caller-supplied malformed positions strict.
- [2026-07-16]: Anime-editor schedule apply must persist and publish only the anime explicitly changed by the DTO, while the refreshed schedule board may normalize untouched sparse legacy placements on read so the UI stays contiguous without inventing extra downstream mutations.
- [2026-07-16]: Equal-order legacy schedule ties follow the same rule as sparse Sundays: untouched records stay byte-faithful on disk, and deterministic normalization belongs in the public board read model plus UI feedback handling, not in extra Apply writes or warning alerts.
- [2026-07-16]: Anime-editor schedule apply serialization must follow the rendered destination sequence from `board.destinations` and card order inside each destination; lexical day-name sorting scrambles Monday→Sunday versus special queues and breaks regression expectations.
- [2026-07-16]: `RunOnce` fan-out must aggregate `processAnime` deltas as they happen; waiting to merge only after each goroutine returns keeps the persisted run row stuck at zero and hides live progress from the UI.
- [2026-07-16]: `RunOnce` live progress must keep the aggregate mutation and `recordProgress` persistence/event fan-out inside the same critical section; unlocking before `UpdateRunProgress` lets an older worker publish stale counters after a newer snapshot.
- [2026-07-16]: When the Go file-size gate trips on `schedule_service.go`, split schedule validation, normalization, and batch-operation helpers into package-private files and pin the behavior with focused schedule normalization tests instead of weakening the gate.
- [2026-07-17]: In Wails WebView2, an `<a target="_blank">` does NOT reliably open the system browser; route external-page opens through `window.runtime.BrowserOpenURL` (guarded, via a source method) so intake/link buttons match the Chapters behavior instead of dead anchors.
- [2026-07-18]: The Go lint entrypoint derives targets from Git-owned Go files, which keeps every tracked profile focused on repository code and prevents recursive tool discovery from linting vendored frontend Go files.
- [2026-07-18]: In the scheduler loop, helper return values must distinguish "config ready" from "idle wait completed" — treating a wake-up from disabled-state polling as a ready config can drop the immediate post-save reschedule and make NotifyConfigChanged look flaky.
- [2026-07-18]: Legacy gateway lint debt in long regression tests is safest to remove by extracting semantic test helpers plus approval coverage for recursive JSON-equality helpers, which preserves real-fixture behavior while cutting cognitive load.
- [2026-07-18]: Revive's package stuttering rule applies to exported aliases too, so package-local bridge types like anime write outcomes must be renamed to neutral nouns (`PatchResult`, `PatchOutcome`, `Writer`) instead of hiding behind alias-only lint workarounds.
- [2026-07-18]: Revive treats infrastructure seam names like `sync.SyncSQLiteProvider` as stuttering exports too, so storage adapters should use neutral nouns (`SQLiteProvider`, `Store`, `Run`) and large stores should shed lifecycle helpers into semantic files instead of carrying size debt.
- [2026-07-18]: Clearing download revive debt safely means renaming the public port/value pair to `download.Store` and `download.Run`, then splitting long service/store tests by behavior helpers so the linter fix does not smuggle runtime changes.
- [2026-07-18]: Once base and advanced Go lint debt both reach zero, the repo-owned hook and CI entrypoints should call `scripts/lint.ps1 -Profile all` so proof stays aligned with the real no-exception enforcement target.
- [2026-07-18]: golangci-lint and dlinter-gcl both default to `--max-issues-per-linter=50`, which hides the true scope of requireDoc debt — use `--max-issues-per-linter 0 --max-same-issues 0` for an uncapped audit before declaring zero.
- [2026-07-18]: A requireDoc wave of 874 undocumented unexported helpers (test fixtures, validation helpers, writer helpers, schedule normalization helpers, wire helpers, recovery helpers) is mechanical volume — batch it with parallel agents grouped by bounded context, finish with the tools/ package edge cases, and always verify with the uncapped run.
- [2026-07-19]: SDD-52 fixes the bridge's English-domain synonym drift by standardizing bridge-owned vocabulary on `episode` while keeping Spanish runtime literals and the storage codec's Spanish keys unchanged; REST/WS payload names went English-canonical afterwards in SDD-56, and residual `NroCapVisto` still survives in bridge-owned read models outside the codec boundary (`internal/anime/episode_service.go`).
- [2026-07-20]: The Daily Board's live "section" is an overlay over animes' current dias, not season-owned data — after Apply schedule an anime's dias becomes a weekday, and the old catch-all default misfiled it as Sin ver. Graduated animes now list under Visto (their last conveyor state) with a day chip; the season registry, not the conveyor, is the immutable log.
- [2026-07-21]: The Anime schedule modal gained a client-only staging (wildcard) area riding the existing duplicateAllowedDestinations exclusion (same mechanism as the Season rail). Semantics chosen with the user: staged moves are discarded on Apply (the anime reverts to its authoritative placements; a warning alert says so) rather than blocking Apply or serializing empty placements — and duplicates now spawn into the wildcard container when one exists, so they never create an invalid draft.
- [2026-07-21]: Refined staging semantics after the user caught the Apply button behaving "randomly": the old rule let a parked card ripple-reindex its former column mates (16 changes if it was #1 of 17, zero if it was last), which is unexplainable UX. New rule: a parked card is treated as still holding its original slot (the before-side diff also ignores staged keys), so parking is always 0 changes and the ripple fires only when the card lands on a real destination. A (?) tooltip on the Staging area title now states the contract: "while parked, nothing has changed". Litmus test used: if a button state can only be explained by describing the implementation, the design is wrong.
- [2026-07-21]: SDD-54 regrouped the desktop rail into LIBRARY/SYNC/SYSTEM (9 items) and killed the Dashboard/Status/Pairing dead surfaces, relocating their content into Devices (Pairing + Connected Devices + Syncing Now + Trigger Reconcile) and Activity (BridgeStatusCard health strip + NetworkPanel) pages; the "page h1 must equal nav label" contract also caught a pre-existing mismatch (`SeasonWorkspace`'s h1 said "Season Workspace" while its nav label was "Season"). Testing an unmocked `/season` route through the real `App` integration test hits the 5s real Wails-binding timeout in `useSeasonWorkspace`'s effect (jsdom has no `window.go`); a dlinter rule blocks raising per-test timeouts to paper over this, so the fix is presetting `seasonStore.setState({ hasLoaded: true, ... })` before render instead of waiting out the real binding poll.
- [2026-07-21]: SDD-55's "legacy adapter" (`internal/anime/legacy`) was never dead Legacy interop — its `wire`/`mapper`/`projection`/`gateway` files were Bridge's own live SQLite persistence codec for `anime_snapshots.snapshot_json`. The legacy breakup is a channel removal (watcher, `.dat` parser, append writer, ownership arbitration), not a code removal: the codec is retained and relocated to `internal/anime/store` with Spanish storage keys intact, while only the file-channel port and its callers are deleted.
- [2026-07-21]: SDD-55 verify gap — a stale wails dev process (launched pre-change) kept the old animes.dat watcher live and made the legacy cut look broken; green gates + grep proved the code but a clean-process runtime check was the missing step.
- [2026-07-22]: In Wails apps, a DTO returned by an app_*.go binding is a frontend contract even if named Mobile* and also served over HTTP — grep app_*.go for contracts.<DTO> before assuming a DTO is mobile-only. SDD-56 exposed this: MobileAnime's Wails binding cascaded ~1950 lines of frontend renames when the name mapping changed, not just backend/HTTP.
- [2026-07-22]: SDD-57 create-anime: `AnimeScheduleOrdering` was generalized for a create-mode batch board purely through additive props (`lockedAnimeIds`, `draftEntries`, `onApplyCreateSubmit`) plus a new `reconcileDraftEntries` helper — but a naive board-change `useEffect` keyed on an unstable inline-array prop (`draftEntries`) caused an actual infinite render loop (`JS heap out of memory`) under vitest, because every re-render recreated the array reference, refiring the effect that in turn changed state. The fix: make the reconciliation helper return the exact same state reference when nothing conceptually changed, so React bails out of the re-render even when the calling prop's identity is unstable — never assume callers can/will memoize array/object props. Also: jsdom has no Web Animations API, so any test that renders (or clicks) a HeroUI `Tabs` component needs an `Element.prototype.getAnimations` polyfill in `src/test/setup.ts`, or the layout effect throws and aborts the render.
- [2026-07-22]: Editor "Watching first" rail was stricter than the Daily schedule board: it used `isWatchingAnime` (active===1 && status===0/Viendo), so a paused (En pausa) anime that still showed in Daily was missing from the editor. The Daily board membership is actually `active===1 && has a scheduled day` (Go `ListEpisodeSchedule` skips `Active==0` then requires `matchingScheduleDay`, ignoring status). Fixed by replacing the predicate with `isScheduledAnime` (active + days.length>0), renaming the tab to "Watching now", and removing the now-dead `isWatchingAnime`/`AnimeEstadoStatus`. Also: the editor footer always showed "Deactivate anime" even for `active===false` records; now it shows "Activate anime" (green, no confirm) wired to the existing `RestoreAnime` binding when `record.frequent.active === false`. Spec drift: openspec/specs/anime-editor/spec.md still says "watching-first" and "`activo=false` MUST be presented as Deactivate anime" — reconcile via SDD.
- [2026-07-23]: Mobile request capture must trust only the authenticated `device.PairedDevice` identity and a sidecar reader must resolve the existing bridge DB path without creating directories, then enforce both `mode=ro` and `PRAGMA query_only=ON` so observability can fail closed without mutating canonical state.
- [2026-07-23]: A bounded capture queue must synchronize the stop transition with non-blocking sends, and capture search must paginate by both `captured_at_ms` and `request_id` so equal timestamps cannot duplicate or skip rows.
- [2026-07-24]: Replaced the `httptest.ResponseRecorder`-only `responseStatus(w)` hack with a real `capturingResponseWriter` (`internal/api/capture_middleware.go`) wrapped at handler entry: the old hack returned a hardcoded 500 for every non-test error path in production, so captured PATCH/reconcile telemetry could never show the real 4xx status or the bridge's own error body. The wrapper records the true status (defaulting to 200 on first `Write`, matching `net/http` semantics) plus a raw body copy bounded by `MaxCapturedBodyBytes` (64KiB) with an explicit `truncated` marker; the captured body is preserved exactly for local debugging rather than projected down to sanctioned keys.
- [2026-07-24]: `specs/observability/spec.md`'s "Additive Capture Schema" requirement text says changelog correlation lookup goes "through the existing correlation table" — there is no such table; correlations have always been the embedded `correlation_json` column, queried via SQLite JSON1 `json_each(...,'$.changelog_ids')`. Code wins (CLAUDE.md rule 2): the design doc for `mobile-request-mcp-debugging-improvements` already records this drift explicitly, and the implementation follows the JSON1 approach.
- [2026-07-24]: `capture-middleware-realtime` replaced ~15 scattered per-handler/per-WS-message capture-record call sites with a single transport-level seam per protocol: an HTTP `CaptureMiddleware` wrapping `/api/*` (writing a pending arrival row before the handler runs, then upserting the terminal row from a `capturingResponseWriter`, panic-safe via `defer`), a `captureWebSocketMessage` decorator around the WS reconcile pump, and a hub sink (`MemoryHubConfig.Capture`) for connect/disconnect/broadcast. Handlers no longer build `CaptureRecord`s themselves — they call `requestcapture.Enrich(ctx)` (a ctx-carried, chainable accumulator seeded by `NewEnrichmentContext` in the middleware) to attach outcome/error_code/payload/correlations, and the middleware merges that enrichment with transport facts (`BuildTransportCaptureRecord`/`MergeEnrichment`) at write time. This means every handler exit path (including an uncaught panic) still produces exactly one terminal capture row without each handler remembering to write one. The hub's capture calls must dispatch on their own goroutine (`go capture(record)`), not synchronously — a slow/blocking `Capture` sink must never delay the hub's own register/unregister/broadcast fan-out, proven by a test with a deliberately-blocking sink. Frontend: `App.emitCaptureTransaction` fires `runtime.EventsEmit("capture.transaction", CaptureRow)` once per persisted queue record (`QueueConfig.OnPersist`), and the Activity view merges pushes via `transactionStore.upsertRows` (dedup by `requestId`, in-place terminal upsert, unseen rows prepended) — because updated rows keep their existing array index, no explicit scroll/selection-preservation code was needed beyond simply not touching `selectedId`/`selectedDetail` in the reducer.
- [2026-07-24]: SDD `activity-devtools-network-view` fixed Activity's Network-tab mislabel: it rendered `ObservabilityLogEntry` (a log), so Status/Duration were always hardcoded `–`. Added an in-process Wails read path (`App.ListCaptureTransactions`/`GetCaptureTransaction` over `requestcapture.NewReader(a.bridgeDB)` — never a second SQLite handle) and a new `TransactionPanel` feature showing real captured HTTP transactions with real status/duration; the old event log lives under Activity's own Runtime Events tab so both lenses stay available under honest names. Drift: `internal/api/contracts` cannot import `internal/observability/requestcapture` (the reverse import already exists via `BuildPatchCaptureRecord`/`BuildReconcileCaptureRecord`), so `contracts.CaptureCorrelations`/`CaptureOperationRef` are a local mirror of `requestcapture.Correlations`/`OperationRef`, mapped at the `app_captures.go` binding boundary. Also: dlinter's `strict-colocation` rule exempts `*.helpers.ts` from the "no root-level `export const`" check but not a same-named main module file — a Zustand `createStore(...)` call must live in `*.helpers.ts` (mirroring the existing `network-store` convention), never in the governed main file, or the lint gate blocks the commit.
- [2026-07-25]: `capture-nomenclature-rename` renamed the `mobilecapture`/`mobile_request_*`/`*_mobile_request*` surface to `requestcapture`/`request_*` (packages, MCP tool names, binary, SQLite tables). Two non-obvious findings drove the design: (1) `persistence.EnsureTableSchema` never calls a table's `Migrate` hook for a table it just created — `tableColumns(db, name)` is empty so it takes the `CreateDDL` branch — so a `Migrate` hook on the renamed `requestCapturesTable()` would have silently created an *empty* `request_captures` while every existing row stayed stranded in `mobile_request_captures`; the rename must run as a dedicated bootstrap pre-step (`ensureRequestCaptureTableRename`, `internal/sync/capture_table_rename.go`) *before* the `EnsureTableSchema` loop, proven by `TestCaptureTableRenamePreservesExistingRows` and the orphan regression `TestCaptureTableRenameNeverOrphansDataBehindAnEmptyTable`. (2) SQLite's `ALTER TABLE ... RENAME TO` carries indexes across under their **old** names, so the five `idx_mobile_request_captures_*` indexes had to be explicitly `DROP INDEX IF EXISTS`-ed for `ensureIndexes` to recreate them as `idx_request_captures_*` — otherwise the database silently kept both the correct and the misnamed index sets. The MCP tool rename (`search_mobile_requests` → `search_requests`, etc.) shipped with **no aliases**: old names are rejected as `unsupported`, forcing a `.mcp.json` update in the same change, because aliasing would have kept the wrong nomenclature alive indefinitely on a surface the sidecar spec deliberately bounds to four tools.
- [2026-07-24]: The 5 slowest frontend suites were not slow tests — they were starved workers. Measured on an idle machine, `AnimeEditorWorkspace.schedule-feedback` ran in 1376ms; in the full 152-file parallel suite the same test took 3056ms (61% of vitest's 5000ms default), so a busy machine flipped the gate red. Two fixes in `frontend/vite.config.ts`, no timeout bump and no test rewrite: (1) a pinned `maxWorkers` — the default of one worker per logical core (20 here) oversubscribes the box, and bounding the fan-out roughly halves per-test latency; (2) a `test.projects` split so the 44 pure helper/store/contract suites run under `environment: 'node'` instead of jsdom — building a jsdom document costs ~690ms per file and those suites never touch one. Result: total suite CPU 458s → 231s (environment 250s → 114s, import 88s → 46s, transform 28s → 8s), wall time unchanged at ~25s, and the worst test dropped 3056ms → ~1.8s (36% of budget, nothing over 2s). `src/infrastructure/**` deliberately stays in the jsdom project because its `*.helpers.test.ts` suites touch `window`; a misfiled suite fails loudly with `document is not defined`. Note `--no-isolate` looks tempting (wall 12s) but breaks 12 files on cross-file DOM leaks — do not use it.
- [2026-07-25]: `activity-transaction-inspect-ui` originally exposed a capture-pipeline honesty gap: there was no truncation signal, successful responses were omitted, and request bodies had no wire-faithful field. The hotfix corrected the contract by storing exact raw `request_body`, exact response bodies, and exact headers for local debugging, while transport-only terminal HTTP rows normalize to `completed` so persisted duration/status never keep ticking as fake in-flight time.
- [2026-07-25]: Activity hotfix: transport-only HTTP terminal rows must never stay visually pending, and the Network-style inspector must preserve exact captured headers/response bodies while runtime events live under Activity's own Runtime Events tab instead of a duplicate top-level Events destination.
- [2026-07-25]: Request/response capture now uses a finite 64KiB per-body budget with explicit persisted state markers (`truncated`, `omitted_too_large`, `omitted_streaming`); pre-auth request capture only reads declared in-budget bodies, dropped terminal enqueues recover through a bounded direct upsert, and the selected Activity inspector row refreshes itself when a runtime terminal update lands for that same request.
- [2026-07-25]: The ignored repo-root `autoreas-request-mcp.exe` drifted behind request-capture SQLite schema evolution, started exiting with `request capture schema mismatch` and closing MCP connections, so local MCP configs now execute current source via `go run ./cmd/autoreas-request-mcp` to remove binary artifact drift.
- [2026-07-26]: Never add linter exclusions for pre-existing code that isn't part of your change. The `.golangci.dlinter.yml` exclusion for `classifyJDStatus` gocognit was reverted because it masked existing debt in `service_hoster_watch.go`. Pre-existing lint findings outside your change scope should be handled separately (baseline, decompose, or exclude in their own PR).
- [2026-07-26]: The frontend notification pipeline was refactored from two separate toast paths (backend `notification.push` events via `useNotificationToasts` + actionable missed-schedule toasts via `useMissedScheduleToasts`) to a unified Chain of Responsibility pattern: every notification source is a resolver hook receiving stable `push`/`remove` callbacks, and `NotificationToasts` owns the single `ToastProvider`, calling each resolver hook in turn and rendering through one `renderAppNotificationToast` helper. Adding a third notification type is one resolver hook plus one call in that component. See `.claude/skills/app-notification-pipeline/SKILL.md`.
- [2026-07-26]: `@nivo/bar`'s `colors` prop passes its values to d3-color for SVG `fill`. CSS custom properties like `var(--color-success)` are NOT parseable by d3 — only real color strings (hex, oklch, rgb) work. The crash was invisible in jsdom tests because jsdom doesn't render SVG fills. Fix: use the literal hex values from the autoreas-theme skill's chart palette. Also discovered HeroUI v3 has no `--color-primary` token — "primary" is just a semantic alias for `--color-accent` (`#0385F7`). Chart palettes are therefore literal hex, declared per feature: `RunProgressBar` sequences `#17C964` / `#0385F7` (or `#71717A` when idle) / `#DB3B3E`, and the Season charts keep their own named constants.
- [2026-07-29]: `mcp-runtime-events-read`'s write path had to solve one hard constraint: `FanoutLogger` freezes its `targets []entrySink` slice at construction, but the persisting sink must be registered before `bridgeDB` exists (logger construction happens before SQLite bootstrap). The fix is a deferred-bind seam — `eventlog.Sink` is constructed and registered as a fanout target immediately (via the new `logger.NewFanoutLoggerWithSinks`, which exports the previously-unexported `entrySink` as `EntrySink` so a `WriteEntry`-only type can be passed), holding an `atomic.Pointer[Queue]` that starts nil. Every `WriteEntry` before `Bind` lands in a bounded 256-entry startup buffer that `Bind` flushes, with the level filter applied at flush time because `PersistDebug` is only known then; only overflow beyond that buffer drops, counted via `Sink.UnboundDrops()`. `Unbind()` is called before `Queue.Stop()` at shutdown specifically so the logging goroutine takes the nil-pointer branch and never contends the queue's stop mutex. Separately: the presence probe for the new `runtime_events` table must live in a standalone `eventlog.NewReader(ro.DB())` constructed *after* `requestcapture.OpenReadOnlyDB` succeeds, never inside it — that function fails closed on any capture-schema mismatch, so probing the events table there would kill the whole MCP sidecar for every bridge database that predates this change.
- [2026-07-29]: `get_correlation_timeline`'s capture-side join cannot be `item.RequestID == correlationID`. The capture middleware mints `requestID := uuid.NewString()` local to one handler invocation (`internal/api/capture_middleware.go`); it never enters a context and never reaches domain code, so no runtime event ever carries it as `Fields.CorrelationID` — the real logged values are `anime.patch:<id>:<ts>`, a download `runID`, or a client-supplied `correlationId`. RequestID equality therefore matches approximately never, and because the spec makes an unmatched correlation a *valid empty result* rather than an error, the tool would have returned a permanently half-empty timeline while reporting success. Fixed by matching the `Correlations` envelope's unique identifiers (changelog/activity/conflict ids) as well, and deliberately NOT device names, operation verbs, or outcomes — those repeat across unrelated requests and would return coincidences. Watch for self-fulfilling fixtures: the original test passed only because it set `RequestID: "corr-1"`, proving the code matched itself rather than reality. A true scalar join needs the correlation id propagated into the capture record — a capture-pipeline change, deliberately out of that slice's scope.
- [2026-07-30]: When you accept a deferred or dropped window at boot, enumerate what actually emits inside it. `tracerBulletRunner.Start()` runs before the only `Sink.Bind` call, which is reached via `configureRuntimeServices` → `configureEventLogQueue`, so *the entire tracer-bullet flow* — the demo sequence used to eyeball whether observability works — is emitted pre-bind and survives only because the sink buffers that window; the first event emitted after `Bind` is `http server listening`, from `startHTTPServer` as the *last* step of `configureRuntimeServices`. The UI shows all seven events regardless, because it reads `memLogger.Recent()` (an in-memory ring buffer attached at logger construction), not `runtime_events`: two sinks, two lifecycles, and a UI/MCP mismatch that looks like a query bug. See `docs/mcp-event-visibility-report.md`.
- [2026-07-30]: `SQLiteStore.pruneOldestBeyondRetention` counts successful writes in an in-memory field seeded to zero by `NewStore`, so it must prune on the first write of every process and then every `pruneEvery` (200) — without that first-write guard a desktop app with short sessions would never prune at all, and the 20 000-row cap would be enforced eventually rather than continuously. The cap itself was reviewed and kept: the only per-operation emitter (`InstrumentedBus.Publish`) is `debug` and filtered by default, and `idx_runtime_events_time` carries the exact `occurred_at_ms DESC, id DESC` keys the prune's `LIMIT -1 OFFSET ?` walks, so it is an index scan rather than a table sort. No age-based retention axis was added — bounding a resource twice to solve an unobserved problem is complexity, not safety.
- [2026-07-30]: `go test -race` cannot run in this repo — the race detector needs a C toolchain on Windows, which is exactly what `modernc.org/sqlite` (pure Go) exists to avoid. The product stays cgo-free; `-race` is a dev-tool gap, not a shippable dependency. Concurrency tests therefore assert *logical* invariants (work not lost, duplicated, or stranded) via the new `internal/testsupport/async` package: `Eventually` sleeps between checks because it waits on real work, while `AwaitState` yields with `runtime.Gosched` because it waits to observe a precise interleaving that a sleep would step over. Be honest about the limit: neither helper can detect a data race, only its consequences. Extracted after the pattern appeared twice (`requestcapture.Queue` stop-guard, `eventlog.Sink` pre-bind buffer); 20 call sites across 13 files now share it, replacing hand-rolled deadline loops whose timeouts had drifted to 300ms/500ms/2s for no reason. The channel-`select`+`time.After` helpers in `internal/download` are deliberately NOT migrated — blocking on a channel beats polling, and they were only caught by a name-based grep, not by shape.
- [2026-07-30]: Mutation-check every test that claims to cover a concurrency guard: delete the guard, confirm the test fails. The first version of `TestSinkConcurrentWriteDuringBindDeliversExactlyOnce` PASSED with `writeUnbound`'s under-lock queue re-check removed — 100 goroutine iterations never produced the interleaving (load nil -> deschedule -> `Bind` completes), so the test proved nothing while looking rigorous. The fix was `TestSinkWriteUnboundEnqueuesWhenBindRacedIn`, which calls `writeUnbound` directly on an already-bound sink to reproduce that state deterministically, and does kill the mutant. Defensive branches the scheduler cannot reach need direct invocation, not stress loops.
- [2026-07-30]: Gremlins mutation testing MUST be run from a node_modules-free checkout (a git worktree), never the repo root: gremlins copies the whole module tree into a temp workdir per worker, so `frontend/node_modules` (~130k files) turns a 16-second run into a 12-minute one that never reports. `exclude-files` does not help — it filters mutant selection, not the copy. A fresh worktree also needs `frontend/dist/index.html` stubbed because `main.go` embeds it. Results here are NOT reproducible run-to-run (three identical runs on `internal/observability/eventlog` gave 94.44%/0%/0% efficacy) because gremlins derives its per-mutant timeout from the ~1.2s baseline while a stalled wait costs `async.DefaultTimeout` 2s × 100 iterations — upstream issues #267 (everything times out on a second run) and #81 (extremely slow on Windows, open since 2022). Trust `NOT COVERED` (it comes from the coverage profile and was identical across all runs); treat KILLED/LIVED as indicative and confirm any specific finding by deleting the guard by hand. No CI threshold gate — it would fail on tool nondeterminism — and `.gremlins.yaml` at the repo root is inert config that nothing reads and nothing runs. See `docs/mutation-testing.md`.
- [2026-07-30]: Mirroring a package copies its code, not its tests. `internal/observability/eventlog/queue.go` was written as a deliberate mirror of `internal/observability/requestcapture`, whose `queue_test.go` covers `Err()`, `setErr()`, the `Stop` deadline branch, the `TryEnqueue` stopping branch and `persist`'s nil-store/error paths — none of which the mirror carried over, confirmed against `go test -coverprofile`. Still open in the same package: `reader_search.go`'s `MalformedRowsSkipped++`/`WarningCount++` never execute, because malformed metadata decodes to nil rather than failing `scanEventRow`, so nothing feeds the skip path.
- [2026-07-30]: Ported `internal/observability/requestcapture`'s `queue_test.go` suite across to `internal/observability/eventlog`, closing the gap gremlins found: `queue.go` went from seven uncovered blocks to zero (every function now 100%, package 91.4%). Two branches are unreachable through the public API and are driven directly rather than left uncovered — `setErr(nil)` (nothing ever passes nil; `persist` and `Stop` both pass real errors) and `Stop`'s `unfinished < 0` clamp (`dequeued` only advances after a record `queued` already counted). Same treatment as `Sink.writeUnbound`'s raced-Bind branch: a defensive branch the scheduler cannot reach needs direct invocation, not a stress loop. All five new guards were mutation-checked by deletion and all five killed their test.
- [2026-07-30]: Gotcha while writing `TestQueueStopClampsNegativeUnfinishedCount`: passing `context.Background()` to `Queue.Stop` makes the test race itself. `Stop` wraps the caller's context in its own 5s budget, and `persist` independently gives each write a 5s context — so with a single in-flight record either can win, and when `persist` wins, `Stop` returns through the clean `<-q.stopped` path and never evaluates the branch under test. The test still PASSED while covering nothing (0.0% on that block, 5.00s runtime). Fixed by passing an explicit 50ms context so `Stop`'s deadline provably fires first; runtime dropped to 0.05s. When a test targets a timeout branch, pin the deadline that must win instead of relying on two equal internal budgets.
- [2026-07-30]: Built `tools/mutationstaged` as the Go counterpart to `frontend/scripts/dlinter-mutation-staged.mjs`, and three separate bugs each produced the same symptom — a run that never finishes — which is why it took four measurements to land. (1) The ooze ignore list was built from *mutable* files only, so `tools/**` and every `_test.go` stayed mutable. (2) ooze derives paths with `filepath.Rel`, which yields **backslashes** on Windows, so a forward-slash-only ignore pattern matched nothing and every exclusion silently dropped — the run looked correctly scoped while mutating all 452 files. (3) The real killer: `fsrepository.LinkAllToTemporaryRepository` walks the repository root and `os.Symlink`s **every file with no exclusions** — `IgnoreSourceFiles` filters what gets *mutated*, never what gets *linked* — so `frontend/node_modules` (~130k files) was symlinked once per mutant. Fixed by materialising the index with `git checkout-index --all --prefix=` into a temp sandbox (1,625 files) and pointing ooze there, which also gives the guard the correct semantics: it judges the staged content, not the working tree. Result: >30min → ~100s for one staged file. Same lesson as gremlins — an exclusion option that only filters selection, not the file-tree copy, is a trap on this repo.
- [2026-07-30]: A `.replace()` in a throwaway patch script silently no-ops when the pattern does not match, and I shipped a "fix" that never applied and then spent another 10-minute run diagnosing the unchanged behaviour. When scripting source edits, assert the replacement landed (`assert old in s`) or use the editor. Also: Python's `io.open(path,'w')` on Windows rewrites LF as CRLF unless `newline='\n'` is passed.
- [2026-07-30]: Mutation testing on the Go side is a PROMPT-DRIVEN TDD step, not a hook — the cycle is RED → GREEN → **MUTATE** → REFACTOR, carried by the `mutation-tdd` skill. Neither tool can carry a deterministic gate on this repo: gremlins scored 94.44%/0%/0% across three identical runs on Windows, and `go run ./tools/mutationstaged` (ooze) costs ~100s for a single staged file against an already ~90s pre-commit gate, while mutating whole files rather than changed line ranges and keeping no incremental cache. The manual check is faster, fully deterministic, and has a better track record: delete the guard, run only that test, confirm it FAILS, `git checkout -- <file>`. It caught two vacuous tests that both the tooling and the coverage number missed. The tools stay documented in `docs/mutation-testing.md` for batch package audits; the discipline stays in the agent instructions.
- [2026-07-31]: SDD-58 backup export cut the `RestorePointMaker` port and the proposed `internal/backup` stdlib-only `checkarchitecture` rule together, because they licensed each other rather than protecting anything independently — the port existed only so the package would not import `database/sql`, and the rule was defended as achievable only *because* the port pushed that import out. The `exportFn func(ctx, io.Writer) (int, error)` seam already guarantees change locality by construction (no parameter through which a table name can reach `internal/backup`), so a layer whose whole job was enforcing an invariant the type already guarantees protects nothing and only makes a future reader believe a hazard exists that never did.
- [2026-07-31]: SDD-59 backup import ships full refresh with no `ListSnapshots`/`ReplaceBaseline` reuse: full refresh removed the only reason import would ever need a prune set — with no partial set to reconcile, there is nothing to compute, so nothing has to be catalog-resident and import can stream exactly like export (`json.Decoder` + `dec.More()`, one record in flight at a time). `ReplaceBaseline` exists specifically to reconcile a *partial* set against a resident catalog, which is precisely the semantic full refresh removed, so reaching for it here would be carrying a tool for a job that no longer exists.
- [2026-07-31]: Wails v2 discards a bound method's resolved struct entirely when it also returns a non-nil `error` — the generated JS wrapper (`wailsjs/go/main/App.js`) just returns `window.go.main.App.X(...)`, and Wails' runtime rejects that promise with only the error's string message on a non-nil error, dropping the second return value. `ConfirmBackupImport(bundleChecksum string) (BackupImportResult, error)` therefore must return a **nil** Go error for a partial group-apply failure once the restore point exists — the failure is reported through `BackupImportResult.ErrorMessage`/`FailedGroup` instead — or the frontend would never see the restore point path or the committed/failed/unattempted breakdown on exactly the case that matters most. A real Go error is reserved for gate failures before the restore point, where there is genuinely nothing structured to report. Any future Wails binding that needs to report a *partial* outcome alongside failure detail must follow the same rule.
- [2026-07-31]: A nil Go slice marshals to JSON `null`, not `[]`, and the frontend blanks with a TypeError the moment it calls `.length` or `.map` on it. This has now bitten three times (download hoster priority, eventlog pages, backup import). The TS type annotation cannot catch it — it describes what we wish Go sent, and nothing verifies that across the Wails boundary. Struct-level tests cannot catch it either: the bug exists only in the serialized shape. Assert on `json.Marshal` output and reject `"field":null` for every slice the frontend reads.
- [2026-08-01]: A root-level episode can make `awaitHosterOutcome` report success before it reaches its recursive-flatten branch, leaving a sibling JDownloader package directory behind; run the folder flattener before every filesystem-success return, not only while polling nested files.
- [2026-08-01]: Bridge shipped with no single-instance guard: `wails.Run` was called with `options.App` lacking `SingleInstanceLock`, so every launch spawned a full second process — its own window AND its own tray icon — and the tray's `OnOpen` only unminimises the window of whichever process owns it, so users accumulated N windows and N tray icons. Fixed by declaring `SingleInstanceLock{UniqueId, OnSecondInstanceLaunch: app.openMainWindow}` so a second launch hands off to the running instance and exits. The options literal was extracted into `buildAppOptions(*App) *options.App` because inline `wails.Run` arguments are untestable. The handler is a named `(*App).onSecondInstanceLaunch` method sitting beside `openMainWindow`/`requestQuit`, NOT an inline closure in `main.go`: `configureTray` already established that the App owns its lifecycle callbacks as method references and `main.go` only wires, so an anonymous closure holding behavior in the composition root broke that symmetry. Wails' `OnSecondInstanceLaunch` fires on the FIRST, surviving instance, which is why it can delegate straight to `openMainWindow`.
- [2026-08-01]: `wails.json` had NO `info.productVersion`, so the `1.0.0` that appeared in `build/windows/installer/wails_tools.nsh` was Wails' hardcoded default, never a declared release version. Releases are now explicit: set `info.productVersion` in `wails.json` and regenerate with `wails build -platform windows/amd64 -nsis`, which rewrites the generated `wails_tools.nsh` defines (`INFO_PRODUCTVERSION`, and the NSIS uninstall-registry `DisplayVersion`). Separately discovered and NOT yet fixed: the built `autoreas-bridge.exe` carries EMPTY Windows file properties (ProductName/ProductVersion/FileVersion/CompanyName all blank) even though `build/windows/info.json` templates them from `{{.Info.ProductVersion}}`. Confirmed pre-existing by building with and without `productVersion` — both produce blank metadata, so the versioninfo `.syso` is not reaching the compile. The installer's version is correct; only the binary's own properties are missing.
- [2026-08-01]: A capture row stuck at `outcome=pending` with a duration counter growing to ~13.7h was NOT a hung handler — it was an orphaned arrival row. `CaptureMiddleware` writes two rows per request under one `request_id` (pending arrival before the handler, terminal row from a `defer`); a process that dies between them strands the arrival as `pending` forever, and nothing revisited it, so every later launch re-read it and the Activity view ticked `now - capturedAtMs` without bound. The tell that it was orphaned rather than slow: the four sibling rows all terminated within ~2s of each other (23:04:01+52628ms, +40429ms, +37152ms, +25455ms all landing ~23:04:53) — a mass simultaneous termination, i.e. shutdown — and the same route answered 404 in 0ms once the process was alive again. Fix is two-sided and both halves are needed: `requestcapture.SweepOrphanedCaptures` rewrites stranded rows to the new terminal `abandoned` outcome at startup BEFORE the HTTP server accepts connections (the one window where no request can legitimately be in flight, which is exactly what makes an unconditional rewrite safe), keeping `http_status`/`duration_ms` NULL because the bridge never observed either; and the frontend stops presenting a pending row as live past `TRANSACTION_STALE_PENDING_THRESHOLD_MS`. `useElapsedClock` had to invert from a boolean to a `(now: number) => boolean` predicate — whether a row still counts as in flight depends on elapsed time, so a boolean computed before the clock existed could never observe the transition and the clock could never stop.
- [2026-08-01]: `HosterPriorityEditor` reordered by mouse in dev and refused to reorder in the packaged app, because `react-aria-components`' `useDragAndDrop` is TWO drag implementations behind one API: the keyboard path runs through react-aria's own `DragManager`, but the mouse path sets `draggable: 'true'` and drives native HTML5 `onDragStart`/`dataTransfer` (`@react-aria/dnd` `useDrag.mjs`) — exactly the native DnD AGENTS.md rule 11 forbids because it does not fire in Wails WebView2. The bug is invisible to `go test`/vitest AND to accessibility review: keyboard reorder (Space, Arrows, Space) works perfectly, so the feature looks healthy from every angle except the one users touch. Diagnostic: if keyboard reorder works and the mouse does not, suspect a native-HTML5 drag path, not the handler. Ported to `@dnd-kit/react` (pointer events) matching `OrderingBoard`; `moveHosterPriorityItem`'s hand-rolled before/after index math was replaced by `applyHosterPriorityOrder(items, orderedKeys)` fed from dnd-kit's own `move` helper, so the library owns index math and we only project key order back onto the wire shape and renumber. This removed the LAST direct `react-aria-components` import, making it an unused direct dependency (it still reaches the bundle as a regular dep of `@heroui/react`), so it was dropped with `bun remove` and a source-wide test now fails if any `src/**/*.tsx?` imports it again. A checked-in test had pinned the OLD import as a dependency-declaration witness — a guard test naming one file as the witness for a rule silently becomes a guard against FIXING that file.
- [2026-08-01]: `AGENTS.md` told every agent to read a doc tree file as mandatory step one and listed a design doc under References, but commit 58b0c98 ("chore: remove obsolete docs") had deleted BOTH deliberately as obsolete without updating the instruction file. Every agent's first action was therefore a guaranteed miss. Change order actually comes from the date-prefixed folders in `openspec/changes/`. When deleting a doc, grep `AGENTS.md`/`CLAUDE.md` for its path — an instruction file pointing at a dead doc is worse than no instruction, because agents burn a turn discovering it and may invent the missing content.
- [2026-08-01]: Released 1.0.2 as a PATCH, not a minor. All three commits since the 1.0.1 bump were `fix`/`docs` (hoster-priority pointer DnD, orphaned capture sweep, the bridge-release skill) with no `feat` among them. The orphaned-capture-sweep commit DID touch `docs/openapi.yaml`, which looks like a wire change and is the signal that would normally force a minor or major — but the entry it added explicitly records "NO REST/WS wire change ... mobile clients require no update". It was announced there only because the project convention is to announce anything that widens the vocabulary MCP consumers read (the new terminal `abandoned` outcome), which is additive and backward compatible. Lesson: an `openapi.yaml` diff is evidence to READ, not a bump trigger by itself — check whether the diff changes a path/field/message shape or merely documents an additive value. Also re-confirmed at 1.0.2 that the built `.exe` carries empty `ProductVersionRaw` and CompanyName/LegalCopyright, because `build/windows/info.json` keys StringFileInfo under the language-neutral `0000` block and declares no `product_version`. Read it with Win32 `VerQueryValue` or Explorer, not PowerShell 5.1's `FileVersionInfo`, which reports the whole block blank and overstates the gap. NOT a regression introduced by bumping — the installer's `INFO_PRODUCTVERSION` is correct.
- [2026-08-02]: Startup tests use both nil and bare `*sql.DB` settings seams; auto-start preference reconciliation must treat either unavailable boundary as a nonfatal, non-observable startup condition while preserving reconciliation and warning behavior with a real SQLite store.
- [2026-08-02]: The Windows login-start visibility correction releases as patch 1.0.3 because it changes startup presentation only and adds no API, wire, or database contract.
- [2026-08-02]: The Today/Episodes panel only updated after navigating away and back because `anime.changed` had no desktop leg. The event fans out on the in-process bus to the realtime hub (mobile WS), the changelog recorder, season availability and the tracer bullet — but the ONLY bus→Wails bridge was `registerDownloadRuntimeEventBridge`, which iterates `downloadRuntimeEventNames` (download.* only). So mobile clients were pushed and the desktop window that owns the database was not; the "fix" of switching sections was just a remount refetch. Added `registerAnimeRuntimeEventBridge` + `contracts.AnimeChangedNotice` (identity only, never the raw snapshot `Payload` — a base64 blob through the WebView on every write buys nothing when the consumer re-fetches its own read model) and a frontend `anime-runtime-source` mirroring `capture-runtime-source`. Lesson: "the backend publishes the event" is not the same as "the UI receives it" — when adding a domain event, check every consumer leg, and remember the desktop frontend is a consumer like any paired device, not an implicit insider.
- [2026-08-03]: Fallow's 44 "unused files" were ~41 `index.ts` barrels, and the three-line `ignorePatterns` fix would have been wrong — the barrels really were unused (40 of 67 had zero production importers) because `generate-feature.js` manufactured one per module and nothing consumed them. Measuring the ratio also went wrong twice first: counting raw imports gave "89% concrete" because 620 intra-module colocation imports and 243 barrel-less targets were in the denominator, when only 325 imports ever had the choice (real split 38.2/61.8); and `rg "from '[^']*/season-store'"` matched the concrete `season-store/season-store` too, since this repo names modules after their folders. Lessons: a large uniform finding category is a signal about the codebase, not the linter — suppression is the last option, not the first; prove your denominator before quoting a ratio; use filesystem resolution, not text matching, for "what does this specifier resolve to"; and check WHO a caller is, since a symbol kept alive only by its own test is still dead. Resolved by ADR-011 (one convention, concrete paths) — 48 issues → 12, zero suppressions. ESLint alone could not guard it: under `moduleResolution: "bundler"` a re-created `index.ts` plus a directory import resolves cleanly and no lint rule can see it, so `check:no-barrels` is a filesystem check in the pre-commit gate. Full writeup: `docs/postmortems/postmortem-fallow-barrel-false-positives.md`.
- [2026-08-03]: Released 1.0.4 as a PATCH. The six commits since the 1.0.3 bump are `fix`/`refactor`/`chore`/`docs` with no `feat`: the desktop `anime.changed` leg, the ADR-011 barrel removal (175 files, but purely internal import-style churn), the public-product-name fix, dead-route deletion, and the Fallow postmortem. Two things that LOOK like bump triggers and are not — (1) `internal/api/contracts/anime_changed_notice.go` is a new contracts type, but it is emitted through `a.emitFn` as a Wails runtime event to the desktop WebView only, never over REST/WS, and `docs/openapi.yaml` is untouched in the range, so mobile clients require no update; (2) `refactor(frontend): remove barrel imports` touches 175 files, but file count is not a semver signal — no exported behavior, wire shape, or DB contract moved. Lesson: size and "it added a contracts type" are both weak evidence; the question is whether a consumer outside this binary can observe the change. Re-confirmed at 1.0.4 that the built `.exe` reports empty `ProductVersionRaw` and CompanyName/LegalCopyright — the known build-metadata gap carried since 1.0.1, NOT a regression from this bump; the installer's `INFO_PRODUCTVERSION` correctly reads 1.0.4.
- [2026-08-03]: Production login auto-start was broken because `go test` — not `wails dev` — owned the HKCU Run value. `ensureRuntimeDependencies` defaults `newAutoStartReconciler` to the real `autostart.NewSystemReconciler()`, and ~30 root-package tests call `app.startup(ctx)`, so any of them reaching `reconcileAutoStart` with a usable settings store wrote `os.Executable()` into the user's real registry. The observed value was `"%TEMP%\go-build1575831245\b001\autoreas-bridge.test.exe"` — a binary deleted the moment the test run ended, so Windows launched nothing at sign-in and Task Manager showed a stranger named `Autoreas-bridge.test`. Lesson: a seam that defaults to the production adapter is not a seam for the tests that forget to override it — when the adapter mutates machine-global state (registry, scheduled tasks, startup folder), the guard belongs INSIDE the adapter, not in each test's setup. Fixed with `isRegistrableExecutable`: registration is refused from any `.test`/`-dev` binary, any `go-build*` path segment, or anything under `os.TempDir()`, and an existing Run value pointing at such a throwaway build is deleted (self-heal) while an installed command is left untouched. Note the symptom was invisible to `go test` itself: every test passed while corrupting the developer's machine.
- [2026-08-03]: Released 1.0.5 as a PATCH. Exactly one commit since the 1.0.4 bump — `e82ef82 fix(autostart)` — and it is a pure defect fix: no new feature, no REST/WS wire change (`docs/openapi.yaml` untouched), no DB or config contract movement. The change is entirely inside `internal/autostart`, and what it alters is WHICH executable path gets written to HKCU Run, not the shape of anything a consumer can observe. Worth noting for future bumps: "it changes machine-global state" is not a semver signal either — the registry is not a published contract, it is the effect the product already claimed to have. One residual to keep in mind when smoke-testing a release: `build\bin\autoreas-bridge.exe` IS registrable under the new guard (a release build output is deliberately not treated as a throwaway build), so launching the freshly-built binary from `build/bin` to smoke-test it will re-point the Run value at `build/bin` instead of the Program Files install. That is intended — it is a real, persistent path, unlike a `go-build` temp binary — but it means a smoke run should be followed by one launch of the installed app to restore the install path. Re-confirmed at 1.0.5 that the built `.exe` reports empty `ProductVersionRaw` and CompanyName/LegalCopyright; that build-metadata gap has been carried across 1.0.1 through 1.0.5 and is NOT a regression from this bump, while the installer's `INFO_PRODUCTVERSION` correctly reads 1.0.5.
- [2026-08-04]: A downloads-root read failure is a runtime configuration failure, never a destination-unresolved anime skip.
- [2026-08-04]: HeroUI Typography only accepts color=default|muted; semantic color needs a Tailwind utility (className=text-warning), and color=warning is a typecheck error.
- [2026-08-04]: A status label every row shares carries zero information: readiness is a boolean, so ready rows get no marker and blocked rows get a one-word tag in a shrink-0 slot, keeping the full sentence for the selection Alert.
- [2026-08-04]: Progressive rails are not interchangeable: useProgressiveListWindow resets its limit when itemCount changes, so a live list like Run history must keep its own reconcile logic and reuse only isNearListBottom.
- [2026-08-04]: Releases now carry CHANGELOG.md: promote [Unreleased] to the version heading and ship it in the same commit as wails.json and the regenerated installer, written in user language rather than commit subjects.
- [2026-08-04]: A second instance losing the :8080 bind set startupErr, which gated startup before startDownloadOrchestration, so readinessService stayed nil and Downloads reported readiness unavailable; only fatal failures may abort startup.
- [2026-08-04]: Release 1.1.1 ships the startup fix: a failed HTTP bind must report through startupErr without aborting startup, so a second instance keeps its local download features.
- [2026-08-05]: Go tools default their parallelism to the logical CPU count, so lefthook parallel:true x 14 jobs had go vet, go test and two golangci-lint passes each claiming all 20 threads and freezing the desktop. Bound each tool, not just the job list. See docs/pre-commit-performance.md
- [2026-08-05]: frontend-filesize-warning inherited eslint.config.js to read one max-lines result, so counting lines built a whole TypeScript program (47s). Contention masked it: confirm a slow job in isolation before blaming starvation. See docs/pre-commit-performance.md
- [2026-08-05]: A maxWorkers value in vite.config.ts silently overrides --maxWorkers on the CLI, so a command-line worker sweep reads perfectly flat and looks like a scaling wall. Tune pool size in the config, not the flag.
- [2026-08-06]: An explicit estado must win over inferred completion: ApplyCompletionStateMachine forced estado=1 whenever a patch carried nrocapvisto>=totalcap, and mobile always sends progress with status, so En pausa/No me gusto silently became Finalizado on fully-watched animes.
- [2026-08-06]: The hoster priority editor saved to site 'default' while GetDownloadConfig and the resolver read 'jkanime', so every reorder was a silent no-op; the backend now owns the site name and echoes it in DownloadConfig.hosterPrioritySite for the editor to write back to.
- [2026-08-06]: Reading an episode number too HIGH is silent and permanent: 'd2ouiemgt90z.mp4' parsed as episode 90, ended a 12-episode catch-up after one file and marked the anime up to date forever, so episodeNumberFromName now accepts only properly delimited digit runs and returns 0 when unsure.
- [2026-08-06]: Stopping a download needed a terminal status of its own: runs now finalize as 'canceled' through a context detached from the run's own cancellation, because writing the terminal row through the cancelled context fails and leaves the row 'running' until startup relabels it 'interrupted'.
- [2026-08-06]: Download configuration stays inside the Downloads route under a Configuration tab, not in Settings: hoster priority is tuned from what run history shows, so splitting them across routes breaks the observe-then-tune loop and turns Settings into a misc drawer. See DownloadsRoute.tsx.
- [2026-08-06]: Auto-rename downloaded episodes is not cosmetic: downloadedEpisodeBaseline returns max(highest, count), and the count half only exists because opaque hoster filenames parse to no episode. Renaming to '<Anime> - NN.ext' makes the highest-episode read authoritative.
- [2026-08-06]: The frontend staged-mutation hook is a silent no-op: dlinter-mutation-staged.mjs lists staged files repo-relative but passes them to git diff from frontend/, where pathspecs resolve cwd-relative, so it always prints 'no added production TypeScript lines' and Stryker never runs.
- [2026-08-06]: An episode that did not land on disk is terminal for its anime. The disk-derived cursor can say 'I have N' but never 'I have 4 and 12', so skipping ahead either fabricates progress or hands the user a menu that creates the gap.
- [2026-08-06]: Release 1.2.0: minor because it adds the download stop button and episode auto-rename. Built without -clean because a live wails dev session holds build/bin/autoreas-bridge-dev.exe; -clean deletes that file and fails with Access is denied.
- [2026-08-06]: A Wails binary logs a full healthy Go startup with a completely blank WebView, so 'the process is alive' is never a smoke test. Render the production bundle in headless Edge (same engine as WebView2) and check #root. See .claude/skills/frontend-render-smoke.
- [2026-08-07]: JD's enabled 'Create Subfolder by Packagename' packagizer rule rewrites any destinationFolder into a subfolder of itself, and since all JD correlation is exact SaveTo matching that broke package lookup, rename and removal at once — send overwritePackagizerRules on every add.
- [2026-08-07]: Adding a member to DownloadRuntimeSource breaks every test fake that spells the interface out: 9 files needed one line each. The cost is real but the interface stays exhaustive on purpose, so a missing binding fails typecheck instead of at runtime.
- [2026-08-10]: jkanime pages its AJAX episode listing 16 at a time; reading only page 1 reported 'latest online 16' and produced a silent false up_to_date that stopped every long series at episode 16 -- walk the paginator, and fail loud on a partial listing rather than under-report the latest episode.
- [2026-08-10]: A test that asserts against the very constant it claims to pin (len(walked) != maxEpisodeListingPages) passes for any value and pins nothing; hand-mutation missed it because it mutated the comparison while the assertion stayed fixed, ooze caught it instantly -- write expected values as literals.
- [2026-08-10]: ast.Inspect calls its visitor with a nil node after every subtree; a Virus decorator that calls node.Pos() on it made ooze report a flawless zero-mutant run in 0.98s instead of crashing -- unit tests all built nodes, so nothing reached it.
- [2026-08-10]: 1.4.1 is a patch: the only user-facing change is the jkanime pagination fix that had silently stopped every anime past episode 16; the mutation-testing work in the same range is internal tooling with no runtime effect.
- [2026-08-10]: MUTATE on Go now means running go run ./tools/mutationstaged, not hand-picking mutants: a self-chosen mutant covers only what you already suspected, and four that all died still hid a test asserting against the constant it claimed to pin. Full writeup: docs/postmortems/postmortem-silent-no-ops.md
- [2026-08-11]: Config rots invisibly when architecture changes: ADR-011 killed the barrels but three .fallowrc.json keys kept pointing at src/**/index.ts (zero files) and the whole rules block restated defaults - a third of the file was dead. Audit config against the tree, not against memory.
- [2026-08-11]: ADR-011 quedo sin guardia: dharness/pure-index-barrel permite un index.ts que solo re-exporta (Pure Barrel Contract) y solo mira lo staged, asi que NO reemplaza a check-no-barrels.mjs, que prohibia el archivo en todo src/. Se borro igual como decision explicita; ver Enforcement status en el ADR.
- [2026-08-11]: dlinter-ts-react era la base entera de ESLint (parser TS, react-hooks, sonarjs, jsdoc) y traia eslint-plugin-react-doctor adentro; al sacarlo hay que instalarlo directo y registrarlo sin activar reglas, si no cada eslint-disable de react-doctor en el codigo pasa a ser error 'rule not found'.
- [2026-08-13]: Un helper que devuelve (valor, changed, err) es una trampa: create_service ignoraba changed y stageaba el BatchOperation CERO que buildScheduleOperation devuelve cuando el reflow no cambia nada, rompiendo el create batch entero con 'anime id is required'. Si descartas un flag, justifica por que.
- [2026-08-13]: Stashing a dependency-touching change to release from a clean tree reverts package.json and the lock while node_modules still holds the new tree: run bun install right after the stash, or the gate fails importing a plugin that is no longer installed.
- [2026-08-13]: dharness sync marca las 6 reglas de dharness-eslint-plugin como residue INERTE: el gate invoca react-doctor con --staged y las reglas de plugin no disparan bajo ese flag (react-doctor 0.5.7). Declarar una regla en doctor.config.json no prueba que corra; el JSDoc de helpers quedo sin guardia.
- [2026-08-13]: El hook de mutacion del frontend no mutaba nada: dlinter-mutation-staged.mjs pasaba pathspecs relativos al repo con cwd=frontend, git resolvia frontend/frontend/... y salia 0 con 'no added production TypeScript lines'. La pista estaba en el gate: 0.4s cuando una corrida real son 30s+.
- [2026-08-13]: Un modulo en features/ que consumen 3 features suele ser un widget mal ubicado: anime-schedule-ordering ademas escondia 4 casts 'as WorkingState' en season. Ver el comentario de boundaries en .dharness/fallow.jsonc.
- [2026-08-14]: A pathspec built only from renamed destinations defeats git rename detection, so a moved file returns as new and its whole content lands in scope: the mutation gate billed three byte-identical moves as new code, 72 of 146 survivors. See docs/mutation-testing.md.
- [2026-08-14]: A test can name the right rule and still be unable to detect its removal: the wildcard-hover test hovered onto an EMPTY wildcard, so the duplicate scan found nothing there either way. Mutation testing finds these; coverage cannot. Design the fixture to discriminate, not just to pass.
- [2026-08-14]: A fat React hook breaches fallow's complexity gate on hook COUNT: useTransactionPanel had 30, of which 13 were store selectors and 5 one-field filter setters. Extracting cohesive sub-hooks (bindings / callbacks / effects) fixes it honestly; the parent keeps its budget for what actually differs.
- [2026-08-23]: react-doctor corre las reglas dharness con SU config, no la del repo: reportó 526 issues contra los 331 reales de `bunx eslint .` — incluye folder-ownership que el repo tiene en "off" y lintea wailsjs/** que ignora. Para deuda dharness el número autoritativo es eslint, no react-doctor.
- [2026-08-23]: `bunx --bun react-doctor` en Windows imprime "No issues found" en stdout mientras stderr dice "Results are incomplete": bun resuelve la ruta C:/... de oxlint como repo git y falla. Un CI que solo lea stdout firma un verde falso. Usar npx -y react-doctor@latest.
- [2026-08-23]: openspec/specs/notifications requires the toast surface to live in frontend/src/app/** and NOT inside a feature; it actually ships in features/notifications/ui/NotificationToasts/. Code wins per CLAUDE.md #2 - drift recorded before the Notification Center delta spec builds on that surface.
- [2026-08-24]: Stryker runs the whole vitest suite instrumented as its dry run at concurrency 4, so a test that passes alone can blow the 5s per-test default and abort the entire mutation step. The error says 'Initial test run timed out', never the test name; dryRunTimeoutMinutes does not help.
- [2026-08-24]: notification.Notification needed Rows/Actions fields for per-anime rows; the struct extension broke 3 pre-existing == comparisons in tests -- adding a slice field always demands a DeepEqual sweep.
- [2026-08-24]: vitest maxWorkers was benchmarked standalone, but the gate runs frontend-heavy beside go-heavy and dharness. At 8 workers four integration tests starve past the 5s per-test budget and fail only inside the hook; 4 workers costs ~17s standalone and makes the gate green.
- [2026-08-24]: tools/checksdd grades whatever .atl/active-sdd-change names. It pointed at an already-finished change, so sdd-gate passed all 13 SDD-60 commits without ever reading its tasks.md. A green gate proves nothing until you check WHICH change it graded.
- [2026-08-24]: A live list refreshed by a push must merge page one on top of the accumulated rows (de-duped by id) and leave the pagination cursor alone: replacing rows throws a user who paged three times back to one, and adopting page one's cursor makes the next load-more re-fetch rows already on screen.
- [2026-08-24]: The notification archived view was unreachable (view pinned to 'active'), stranding RestoreNotifications and the 'archived-empty' state; its switcher uses plain Buttons, not ToggleButtonGroup, because single-select renders role=radio ('narrow this list') and view switching is navigation.
- [2026-08-24]: A component with perfect unit tests and zero production importers is indistinguishable from a wired one. NotificationDetail shipped that way behind 7 green test files; the route test that could have caught it mocked the panel out and asserted a div rendered.
- [2026-08-24]: Two guards that each fully block the same case are individually unkillable: mutation found both bindRowActions survivors. Keep one guard, or replace the boundary with a clamp (min/max) so the unprovable comparison stops existing at all.
- [2026-08-24]: A notification's source and kind are different axes: source is the bounded context (download), kind the event within it (download.run_stopped_early). Kind arrived as a nullable column, so it must scan through sql.NullString or the first read after an upgrade degrades the whole inbox.
- [2026-08-24]: notification.push carries ActionSpec without the minted action ids a press needs, and RecordID is never populated, so a backend toast can never render a working button: missed_schedule persists through the Go Notifier for the record, while its dedicated resolver keeps the toast.
- [2026-08-24]: center.Executor refuses on action.ExecutedAtMS != 0 before resolving a handler and never calls Repeatable(), so IntentHandler.Repeatable is dead contract today: a copy-hoster token registered as repeatable would still refuse the second press.
- [2026-08-24]: A successful download run showed an empty notification pane because the collapse rule folded any anime that was not failing -- including the ones that downloaded episodes. On a clean run, what downloaded IS what needs showing; only checked/up-to-date/skipped anime collapse.
- [2026-08-24]: A toast added to an app-owned HeroUI ToastQueue instance cannot be closed with the module-level toast.close: two queues, two key spaces, and the wrong one silently no-ops -- every mocked test stayed green while remove() closed nothing. Pin close on the real queue, not on the call.
- [2026-08-25]: A backend event nobody subscribes to is a silent no-op that still reports success: navigation.open emitted notification.navigate with zero consumers in frontend/src, so Open Downloads pressed green and never moved. Grep the consumer, not just the producer, before calling an event wired.
- [2026-08-25]: The detail pane archived a record and told no one, so the list kept showing what the Archived tab already had. notification.archived carried the ids all along and only the toast controller listened: subscribing the list to it covers every archive origin, where a drilled callback covers one.
- [2026-08-25]: Mark unread moved the rail badge and left the master-list row rendering as read: one screen telling both stories. Read state is patched onto the loaded rows in place, never by refetching -- a refetch would evict the row from under the cursor on the Unread tab and rewind paging for a dot.
- [2026-08-25]: A Stryker ArrayDeclaration mutant that puts a constant literal into a useCallback dep array is EQUIVALENT, not a test hole: useCallback compares deps with Object.is and a literal is the same value every render. Emptying a non-empty dep array is the real one -- that mutant is worth chasing.
- [2026-08-25]: Un doc muy referenciado no es un doc vigente: docs/architecture.md tenia 31 referencias entrantes y su primera descripcion de dominio seguia siendo el file watcher sobre animes.dat que SDD-55 retiro. El conteo de citas mide dependencia, no vigencia; auditar contra el codigo, no por popularidad.
- [2026-08-25]: A notification CTA must answer what the user wants NOW given the outcome, not what event fired: run_completed offered Run this anime again. And the CTA key is the ROW's own status, not the notification kind -- one run lists failed, downloaded and up-to-date anime side by side.
- [2026-08-25]: Antes de borrar un doc, grep del nombre base, no de la ruta: repository_policy_test.go armaba la suya con filepath.Join("docs","verification",...), asi que buscar la ruta literal dio cero referencias y el borrado rompio la suite. Un test puede pinnear un doc sin que su ruta aparezca entera.
- [2026-08-25]: Announcing work before resolving what it will touch leaves a notification with no subject: RunOnce notified run_started, then listActiveAnimesToday ran inside execute. Hoist the query above the notify; moving the notify down instead would lose the announcement on a failed selection.
- [2026-08-25]: Kind and Correlation ID sat in the notification pane as monospaced values: Kind restates the title in wire vocabulary and keys no filter, and a correlation id the user cannot paste anywhere is an argument for a link, not a printed token. Debug fields earn UI by becoming behaviour.
- [2026-08-25]: Adapter and Notifier shared a signature, so the compiler could not tell a presentation sink from a notifier and identity had nowhere to travel. Adapter now takes a Delivery envelope; the producer-facing port still takes a producer-owned value, because a producer never knows a record id.
- [2026-08-25]: Windows toast tests swap package-level seams, so t.Parallel() makes them read each other's pushes and fail as if the field never rendered. The existing tests omit Parallel for exactly that reason; a shared global is the one place the default of parallelising every test is wrong.
- [2026-08-25]: HeroUI sizes .toast-region to its content with no max, and .toast__content is a column flex container with no min-inline-size:0 -- so one unbreakable anime title sets the toast width and truncate inside it can never fire. Cap the surface, unblock the shrink, then wrap.
- [2026-08-25]: HeroUI puts a toast action beside the content, not under it: .toast is flex-row and .toast__action only stacks below sm. With rows present that halves the content column. The approved artboard draws a footer, so the actions belong inside ToastContent.
- [2026-08-25]: Removing Kind and Correlation ID left buildNotificationMetaEntries always empty and NotificationDetailMeta always null, so the block was deleted rather than kept as an empty shell. A component that can only ever render nothing is dead code wearing a future's clothes.
- [2026-08-25]: On a Windows toast a repeated line is not cosmetic: it shows a handful of lines then clips, so a single-anime run naming the same title in body and folded row pushed the real detail off the notification. Fold only what the body does not already say.
- [2026-08-25]: A browser-based layout guard proved my own CSS fix inert: HeroUI sets --toast-width as a min-width, and in CSS min-width beats max-width, so the cap I added could never fire. What fixed the overflow was dropping truncate; the stylesheet rules were dead the whole time.
- [2026-08-25]: The run-history selection lived in the shared download store, not in component state as I had claimed, so the See this run deep-link only needed the route to read a runId param. Measure where state lives before sizing the work that depends on it.
- [2026-08-26]: wails build -clean fails while a wails dev session holds build/bin/autoreas-bridge-dev.exe. Dropping -clean builds both release artifacts correctly and leaves the dev binary alone; verify by artifact timestamp and INFO_PRODUCTVERSION rather than killing the user's dev session.
- [2026-08-26]: Release 1.4.2 promoted [Unreleased] by RENAMING the [1.4.1] heading instead of inserting a new one above it, silently refiling everything 1.4.1 shipped under 1.4.2. Promote by inserting; never rename the previous heading.
- [2026-08-27]: The Go mutation wrapper is gone; MUTATE is now `ditto staged`, installed rather than vendored, and `github.com/gtramontina/ooze` is out of go.mod. The measurement that justified deleting it: two staged changes run through both paths reported the same mutants, killed and survived, with the same survivors by mutator — and the second case moved both numbers together, so the agreement was reproducible rather than a constant. What the migration then exposed is the part worth remembering: **the root package had been reporting 9 of 9 mutants killed, a perfect 1.00, for a package that never compiled.** A sandbox was mirrored as symlinks, Go refuses to embed an irregular file, `main.go` embeds `frontend/dist` — so the build failed, and ooze reads a failing command as a killed mutant. Each mutant "died" in 0.9–2.3s where one real run of that suite takes 4.95s. The truth is 8 killed and one survivor, `app_defaults.go:43:26 → Comparison Replace`, invisible until now. Two things fixed it and both were needed: ditto v0.5.0 copies into its sandbox instead of linking (a symlink is written through to its target, and a hard link shares the inode, so neither is a sandbox), and `frontend/dist/index.html` is now tracked so the index builds on its own — a clean clone did not compile before. See `docs/mutation-testing.md`.
- [2026-08-27]: Two of this repository's gates were failing for the same reason, and neither was a code defect: **a generated artifact that the build requires and a clean checkout does not have.** `frontend/dist` is embedded by `main.go`, is gitignored, and its absence made `go build ./...` fail on a fresh clone — fixed by tracking one placeholder, which needed `.gitignore` to say `frontend/dist/*` rather than `frontend/dist`, because git cannot re-include a file whose parent directory is excluded. `frontend/wailsjs` is generated by `wails generate module`, is gitignored, and its absence made **105 of 240 frontend test files fail to resolve their imports** — a suite that looks catastrophically broken and is one command from green (2057 tests pass after it). Both look like defects and are neither. When a gate fails on a checkout nobody changed, ask what the build needs that git does not carry before reading the failure as a bug.
- [2026-08-28]: Corrects the two entries above, which described tracking `frontend/dist/index.html` as the fix. It worked and it was the wrong shape: a tracked file inside a build-output directory is rewritten by **every** build, measured over two consecutive ones, so `git status` never comes back clean. The clean-tree alternative — a tracked `.gitkeep` plus `emptyOutDir: false` — trades that for stale bundles, because not emptying the directory is the same property that lets a placeholder live; measured by planting a stale `assets/index-OLDHASH00.js` and watching it survive. Neither is good, and ditto v0.6.0 removed the need to choose: **`.ditto.json` names the generated paths git does not carry** and they are copied into the sandbox after the index is materialised. Naming a path git *does* track is refused, because the index version is the one a staged run measures. So nothing lives in `frontend/dist` any more, and the same file names `frontend/wailsjs` too — the second generated artifact this repository needs and does not commit. The general lesson from the entry above stands and is now enforceable rather than remembered.
- [2026-08-28]: Upgrading Go: run the suite under the new toolchain BEFORE editing the go directive, so compiler/stdlib breakage stays separable from language-version breakage. And force -count=1 — the first 1.27 run reported 41 packages ok almost entirely from cache.
- [2026-08-28]: go fix on Go 1.27 needs three corrections: its embedlit modernizer emits code that does not parse, one pass is not a fixpoint, and it leaves behind the helpers it just made uncallable. go build misses all of it because it skips test files; go vet and the linter catch it.
- [2026-08-28]: Verifying Go lint with scripts/lint.ps1 -Profile base is false confidence: the commit gate runs -Profile all and gocognit lives only in the dlinter plugin. Base reported 0 issues while the gate failed on cognitive complexity 17.
- [2026-08-28]: Every Go gate green is not proof the app builds. The Wails CLI loads packages with its OWN pinned x/tools, and v2.12.0 pins v0.30.0, which cannot read Go 1.27 export data. wails dev and build both failed while vet, tests and lint were clean. Fixed by moving to wails v2.15.0.
- [2026-08-28]: The bridge port travels inside the pairing QR, so mobile follows a port change by itself. Of 65 occurrences of 8080 across both repos only 3 were production: the server default plus mobile's manual-entry default and placeholder. The rest were fixtures and docs.
- [2026-08-28]: Choosing a service port on Windows: check netsh int ipv4 show dynamicport tcp (ephemeral starts at 49152 here) and show excludedportrange. A port inside either can be taken before your server binds it. Then check IANA and what is actually listening.
- [2026-08-28]: Mutation testing found dead code, not just missing tests: err != nil in normaliseAddr was unreachable because Atoi returns 0 on failure and usablePort already rejects 0. And rejecting port 70000 pins nothing — the limit moved to 65534 and 65536 unnoticed. Assert at the edge.
- [2026-08-28]: Release 1.6.0 stayed minor, not major: the port move breaks a paired phone until it re-pairs, but the REST/WS contract did not change and no new APK is needed, since the QR carries the port. Semver tracks the API, not the deployment address.
- [2026-08-29]: Only s.logf reaches SQLite and the request MCP; s.publish fans out to the in-memory bus and dies there. A field added to an events/event.go struct is invisible to forensics. SDD-61 instrumented the download orchestrator through logf metadata for exactly this reason.
- [2026-08-29]: A guard written to constrain one change must not enter the permanent spec as an unconditional MUST. SDD-61's 'the verdict MUST remain dead' was right for an instrumentation-only change and became a standing order to keep the bug. Scope such requirements to their change.
- [2026-08-29]: ditto staged scopes to staged PRODUCTION lines, so a test-only change generates ZERO mutants and exits 0. That is not 'all killed', it is 'none created'. Prove test-only work with hand-mutations of the production code the tests cover.
- [2026-08-29]: Bridge's AddAndStart(autostart=true) makes JD run DownloadWatchDog.startDownloads(), starting the WHOLE download list, not only the links just added; the 'Bridge sweeps the LinkGrabber' report was closed not-reproducible after 5 experiments against the live JD.
- [2026-08-29]: A row CTA promising 'ready to watch' must land where episodes are watched (/today), not on the anime record (/catalog/detail/{id}). 'L2 never navigates to a generic context' optimized scope purity and left the user one navigation short; corollary 2 now names the exception.
- [2026-08-30]: wails.json records which version was declared; only a git tag records which commit shipped it. bridge-release now tags vX.Y.Z on the release commit after the gate passes -- local only, pushing stays opt-in.
- [2026-08-30]: append([]T{}, nil...) returns an EMPTY NON-NIL slice, laundering absent into clear-everything: editor_service.go did that to the placements patch and every editor save silently wiped the anime's schedule days for six weeks. See docs/reports/debugging-metrics-report.md.
- [2026-08-30]: A check that finds nothing must prove it looked: checktruncation scanned a pre-SDD-56 DB whose snapshots say 'dias' not 'days', matched zero rows and printed 'clean'. It now errors when no committed row carries a known collection field.
- [2026-08-30]: A field nobody is forced to fill will be empty: six producers of AnimeChangedEvent never set ChangedFields, so the changelog logged [] for six weeks. Derive it where both states are already in hand, not where a caller must remember. See SDD-64.
- [2026-08-30]: logger.Warnf takes (domain, format, args) - a call site passed its whole sentence as the domain, poisoning runtime_events.domain with prose. Nothing failed because nothing asserts a domain. Same class as the tracer bullet's prose-derived domain.
- [2026-08-30]: A React state updater that reads a ref runs lazily: the effect overwrote previousTotalRef before the queued updater read it, so SDD-65's 'fully revealed stays revealed' fired only when React computed eagerly. Read bookkeeping refs into locals before queueing.
- [2026-08-31]: A list-wide ticking clock inside a row memo costs O(rows) per tick: Transactions burned 98% of a second at 250 loaded rows while Runtime Events, whose memo has no clock, stayed fine. Move a per-row live value into the cell that renders it.
- [2026-08-31]: Table.LoadMore rebuilds its IntersectionObserver on every collection change and counts itself visible a container height below the fold, so a rail that appends what it fetched pages itself to exhaustion. Live rails trigger on onScroll + isNearListBottom instead.
- [2026-08-31]: A fake whose every page omits nextCursor never exercises pagination: the same Table.LoadMore runaway shipped twice (Transactions, then Notifications) because no test could reach page two. Fix a library-primitive bug, then grep the tree for its other call sites.
- [2026-08-31]: An Activity detail card scrolled the whole window sideways: a bare 1fr grid track is minmax(auto,1fr), so a content-based minimum beats the container. jsdom cannot see a box, so the guard belongs in frontend/scripts/layout-smoke.mjs, never in vitest.
- [2026-08-31]: Cap the CARD, fill the panes: a fixed max-h on a scrollable pane cannot grow into a card the grid stretched (261px dead band), and a flex-1 pane needs the card capped or its content height grows the row. min-h-0 at every level, and measure at two viewports: at 1280 a re-added max-h-64 is inert.
- [2026-08-31]: An sr-only suffix inside a control joins the visible label with no separator: the Today tab announced 'Sundaytoday'. Give the hidden phrase its own leading comma rather than trusting JSX whitespace.
- [2026-08-31]: 1.8.0 is minor, not major: changed_fields starts carrying values but its type, required-ness and empty-array default are unchanged, so a client that ignored it is unaffected. New behaviour on an existing field is not a wire break.
- [2026-08-31]: wails build -clean dies with 'unlinkat autoreas-bridge-dev.exe: Access is denied' while a wails dev session holds build/bin. Close the dev app before a release build; the error names a file, not the running process.
- [2026-09-02]: A Linux tarball holding a bare ELF is not a deliverable: Ubuntu 22.04 refused it with libwebkit2gtk-4.1.so.0 missing and nothing named the fix. A .deb declares the dependency so apt resolves it; building on the newest distro also sets the compatibility floor there.
- [2026-09-02]: A .deb carrying a .desktop and an icon still lists as a generic gear in GNOME Software: the pre-install listing comes from AppStream metainfo, the post-install icon from the desktop shell. Two mechanisms, and shipping only the second looks like a broken package.
- [2026-09-02]: AppStream never inherits <icon> from the .desktop, and type=stock resolves against hicolor, not /usr/share/pixmaps. Shipping only the pixmaps copy is why the app menu showed the right icon while the software store drew a generic package.
- [2026-09-02]: Two 4286-byte .ico files looked identical by size and were not: resources/tray-icon.ico held pre-rebrand artwork for months because nothing ever compared it to build/appicon.png. Generated binaries need a -check gate, not a naming convention.
- [2026-09-02]: A Capacity-1 queue whose drain goroutine starts in the constructor holds TWO records, not one: run() receives from the channel before it blocks in the store, freeing the slot. Asserting the second enqueue is dropped is a race; wait for a store-entered signal first.
- [2026-09-02]: A lefthook glob decides IF a job runs, not WHAT it runs over: golangci-lint globs *.go but scripts/lint.ps1 lints every .go in the tree, tracked and untracked-not-ignored. A peer session's WIP failed my commit of unrelated files. Read the run:, not the glob:.
- [2026-09-02]: Three agent sessions can share ONE working tree here, not clones: git worktree list returns a single entry, so a peer's uncommitted files show up in your own git status. git add -A swallows their work; git commit --only -- <paths> commits yours without touching the shared index.
- [2026-09-02]: A commit that only renames identifiers inside a script nobody invokes is a dead-code signal, not maintenance: the SDD-52 intake probes were kept in sync with a column rename for two months while no hook, script, CI job or doc ever ran them.
- [2026-09-02]: test:mutation:staged says 'no staged production TypeScript lines' for anything outside frontend/src: isProduction requires the src/ prefix AND a .ts/.tsx extension, so frontend/scripts/** is never mutated whatever you rename it to. The line is true and reads like a pass.
- [2026-09-02]: A repo-wide transformer that compiles and passes every gate can still make the code worse: mine flattened a deliberately one-parameter-per-line signature into 160 characters just to group two params. No check here would ever have failed that. Read the diff, not only the green.
- [2026-09-02]: Go's single-method-interface idiom and this repo's ports vocabulary never conflicted: Repository and Gateway are multi-method by nature, so the -er rule only governs the gap they leave. gateway_contracts.go had already got it right, with WriteBaseStore beside ConflictWriter. See ADR-017.
- [2026-09-02]: A release guard reading back `go version -m | grep bridgeVersion=X` cannot fail: that echoes the -ldflags the job just passed, not the stamped symbol. A binary reporting "dev" passed it. Assert the linker s `<pkg>.bridgeVersion.str` on a non-stripped companion build. ADR-018.
- [2026-09-02]: Renaming the Wails binding namespace touches THREE shapes and grepping import paths finds one: the runtime global `window.go.<pkg>.App` was in 16 files, two production. tsc blesses it because src/test/setup.ts declares the type. Only the test suite caught it. ADR-018.
- [2026-09-02]: `dharness check` cannot run from a git hook inside a worktree: GIT_DIR points at .git/worktrees/<n> and it resolves frontend/frontend. It passes standalone. Committing from a worktree needs LEFTHOOK_CONFIG with a copy that drops the .dharness extends.
- [2026-09-02]: A merge commit runs `pre-merge-commit`, never `pre-commit`, and nothing was installed there until 2026-09-02: a signature change on one branch and a new caller on the other only fail together. `lefthook validate` does not check hook names; `lefthook install` does.
- [2026-09-03]: Keep live-rail integration fixtures at the smallest state that proves push, selection, and scroll preservation; duplicating hook-level pagination inflated HeroUI reconciliation until gate contention crossed Vitest's timeout.
- [2026-09-04]: El job golangci-lint de lefthook corre un set de linters mas estricto que un 'golangci-lint run ./pkg/...' manual: el manual dio 0 issues y el hook fallo con gocognit y nestif. Verifica contra el hook, no contra la corrida manual.
- [2026-09-04]: ditto staged con nada de produccion en el indice imprime CERO salida y sale 0: un pase que no midio nada, indistinguible de uno real. Verifica siempre con --dry que el alcance incluya los archivos de produccion antes de creerle al score.
