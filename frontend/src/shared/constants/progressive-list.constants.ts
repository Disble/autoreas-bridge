/** Rows rendered before the first scroll; the list grows by PROGRESSIVE_LIST_LOAD_BATCH. */
export const PROGRESSIVE_LIST_INITIAL_COUNT = 20;

/** Extra rows appended each time the user scrolls near the bottom of a rail. */
export const PROGRESSIVE_LIST_LOAD_BATCH = 20;
