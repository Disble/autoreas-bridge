/** One second in milliseconds, the unit `formatRelativeTimeAgo` reduces an age to first. */
export const MILLIS_PER_SECOND = 1000;

/** Seconds in a minute — `formatRelativeTimeAgo`'s first bucket boundary. */
export const SECONDS_PER_MINUTE = 60;

/** Minutes in an hour — `formatRelativeTimeAgo`'s second bucket boundary. */
export const MINUTES_PER_HOUR = 60;

/** Hours in a day — `formatRelativeTimeAgo`'s last bucket boundary. */
export const HOURS_PER_DAY = 24;
