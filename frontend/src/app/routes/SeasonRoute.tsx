import { SeasonWorkspace } from '../../features/season/ui/SeasonWorkspace/SeasonWorkspace';

/**
 * SeasonRoute mounts the season workspace as its own routed surface.
 */
export function SeasonRoute() {
  return <SeasonWorkspace />;
}
