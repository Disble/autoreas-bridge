/** Field label for the downloads root input. */
export const DOWNLOADS_ROOT_LABEL = 'Downloads root';

/** Helper copy explaining how the root is used. */
export const DOWNLOADS_ROOT_DESCRIPTION =
  'Base folder for new season animes. Each new anime defaults to this folder plus its name; you can change it per anime when creating.';

/** Native folder-picker dialog title. */
export const DOWNLOADS_ROOT_PICKER_TITLE = 'Choose downloads root folder';

/** Toast message shown after the root is persisted. */
export const DOWNLOADS_ROOT_SAVED_MESSAGE = 'Downloads root saved';

/** Fallback error copy when persisting the root fails unexpectedly. */
export const DOWNLOADS_ROOT_ERROR_MESSAGE = 'Could not save the downloads root.';
