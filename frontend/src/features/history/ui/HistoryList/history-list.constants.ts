/** Label shown when the History surface is empty or still loading. */
export const HISTORY_LIST_EMPTY_TITLE = 'No history yet';

/** Helper message shown alongside the empty title. */
export const HISTORY_LIST_EMPTY_MESSAGE = 'No animes are currently in progress or have repetition history.';

/** Accessible label for the History list region. */
export const HISTORY_LIST_LABEL = 'Anime history';

/** Fallback shown for a repetition entry whose `fechaRepeticion` is null/absent. */
export const HISTORY_LIST_UNKNOWN_DATE_LABEL = 'Unknown';
