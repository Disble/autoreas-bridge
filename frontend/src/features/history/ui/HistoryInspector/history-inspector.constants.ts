/** Title of the inspector's empty state while no history row is selected. */
export const HISTORY_INSPECTOR_PROMPT_TITLE = "No episode selected";

/** Description of the inspector's empty state, naming the gesture that fills it. */
export const HISTORY_INSPECTOR_PROMPT_DESCRIPTION = "Select an episode to see its anime.";

/** Accessible label for the inspector loading region. */
export const HISTORY_INSPECTOR_LOADING_LABEL = "Loading anime detail";

/** Title of the inspector error alert. */
export const HISTORY_INSPECTOR_ERROR_TITLE = "Could not load the anime detail";

/** Label of the button opening the inspected anime's detail. */
export const HISTORY_INSPECTOR_OPEN_LABEL = "Open anime detail";

/** Accessible label for the watched-episodes progress bar. */
export const HISTORY_INSPECTOR_PROGRESS_LABEL = "Watched episodes progress";

/** Label heading the last-watched date row. */
export const HISTORY_INSPECTOR_LAST_WATCHED_LABEL = "Last watched";

/** Label heading the added date row. */
export const HISTORY_INSPECTOR_ADDED_LABEL = "Added";

/** Alt text of the inspector cover image resolved through the shared cover hook. */
export const HISTORY_INSPECTOR_COVER_ALT = "Anime cover";

/** Heading of the 3 most recent episodes list (design D8). */
export const HISTORY_INSPECTOR_RECENT_TITLE = "Recent episodes";

/** Label heading the watched-episodes count above the progress bar. */
export const HISTORY_INSPECTOR_WATCHED_LABEL = "Watched";
