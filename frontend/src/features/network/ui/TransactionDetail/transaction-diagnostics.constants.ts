/** Section heading above the projected sync diagnostics values in the Request tab. */
export const TRANSACTION_DIAGNOSTICS_REPORT_HEADING = 'Sync diagnostics report';

/** Row value shown when a diagnostics report explicitly carries `previous_cycle: null`. */
export const TRANSACTION_DIAGNOSTICS_PREVIOUS_CYCLE_NONE = 'No previous cycle recorded.';

/** Notice shown when a diagnostics capture body cannot be parsed as JSON at all. */
export const TRANSACTION_DIAGNOSTICS_UNPARSEABLE_BODY_NOTICE = 'The captured body could not be parsed as JSON.';
