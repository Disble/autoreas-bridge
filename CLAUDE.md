# CLAUDE.md

This repository uses `AGENTS.md` as the primary project instruction file.

## Read First

- `AGENTS.md`
- `openspec/config.yaml`
- `.atl/skill-registry.md`

## Project Notes

- Follow the SDD workflow and active change artifacts under `openspec/changes/`.
- Load `bridge-testing` for bridge test work.
- Load `bridge-debugging` for regressions and boundary investigation.
- Prefer real fixture validation with `resources/autoreas-data/animes.dat` when parser compatibility matters.
