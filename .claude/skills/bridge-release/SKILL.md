---
name: bridge-release
description: "Trigger: release, version bump, semver, build the installer, regenerate NSIS, ship a new build, deploy, GitHub Actions release, tag a version. Bump wails.json productVersion and ship through CI or locally."
license: Apache-2.0
metadata:
  author: autoreas-bridge
  version: "2.0.0"
  scope: project
  updates: living
---

# Releasing autoreas-bridge

The version is declared in **one** place: `info.productVersion` in `wails.json`.
Everything else is generated from it.

There are two ways to produce a build, and they share every precondition below.
Only the last step differs: who runs `wails build`, and where the artifact lands.

| Path | Runs the build | Artifact lands in | Use it for |
|---|---|---|---|
| **CI** (default) | GitHub Actions, on a pushed tag | a published GitHub Release | anything users install |
| **Local** | your machine | `build/bin/` | smoke-testing a build before tagging |

A local build is a rehearsal, not a release. It ships nothing and is never the
answer to "cut a release" on its own.

## Branch Model

`dev` carries development. `main` carries deployments. Adopted 2026-09-01.

- Work lands on `dev`. Never commit development directly to `main`.
- A release exists only after `dev` is merged into `main`.
- **Tag the commit on `main`**, never a `dev` commit. `release.yml`'s `guard`
  job refuses to build a tag that is not an ancestor of `main`, so a tag placed
  on `dev` fails the run instead of publishing a build from an unmerged state.

## Hard Rules

- **Every release updates `CHANGELOG.md`.** Promote `## [Unreleased]` to the new
  `## [X.Y.Z] — YYYY-MM-DD` heading and leave a fresh empty `## [Unreleased]`
  above it. Commit it with `wails.json` in the same commit — the version and the
  notes ship together or the release is a lie. CI enforces this: a tag whose
  version has no CHANGELOG section fails rather than publishing empty notes.
- **Write the changelog for the user, not the git log.** Entries say what changed
  for someone using the app, in the app's own vocabulary. `feat(download):
  partition solo download rail` is a commit subject; "Downloads now separates
  anime that are ready to download from those that are blocked" is a changelog
  entry. Never paste commit subjects.
- Group under Keep a Changelog headings — `Added`, `Changed`, `Deprecated`,
  `Removed`, `Fixed`, `Security` — plus `Internal` for changes with no
  user-visible effect. Omit headings with nothing under them.
- **Call out wire changes explicitly.** REST/WS contracts have mobile consumers;
  a wire-affecting release says so under its heading, and a breaking one is a
  major bump announced in `docs/openapi.yaml`.
- **Corrections are patch releases.** Never re-cut, move, or force-push a tag
  that has already been published — anyone who downloaded it keeps a build that
  no longer matches the tag. Bump to the next patch instead.
- Never hand-edit `build/windows/installer/wails_tools.nsh`. It is generated by
  `wails build -nsis`; edits are silently overwritten on the next build.
- Never commit `build/bin/**`. It is gitignored — artifacts ship, they do not
  get versioned.
- A missing `info.productVersion` means the build silently falls back to Wails'
  hardcoded `1.0.0`. Absence is never a declared version — treat it as unset.
- Never `--no-verify`. The gate takes ~90s–300s; give `git commit` a timeout
  of at least 300000 ms. (A workflows-only or docs-only commit skips every job
  and finishes in well under a second — that is the glob working, not the gate
  being bypassed.)
- The tag format is `v` + the exact `info.productVersion`, no suffix — `v1.7.0`,
  not `1.7.0` or `release-1.7.0`. This is a Go module repo, and `v`-prefixed
  semver is the only form Go tooling recognises.

## Decision Gates

| Change shipped | Bump |
|---|---|
| Bug fix, no contract or UX change | patch — `1.0.0` → `1.0.1` |
| New feature, backward compatible | minor — `1.0.1` → `1.1.0` |
| Breaking wire/DB/config change | major — `1.1.0` → `2.0.0` |

Wire contracts have mobile consumers: a breaking REST/WS change is major and
must also be announced in `docs/openapi.yaml`.

## Shared Preconditions

Do these once, on `dev`, regardless of which path ships the build.

1. Read the current version: `rg productVersion wails.json`. If absent, the
   effective version is `1.0.0`.
2. Review what actually shipped since the last release bump
   (`git log --oneline <last-release-sha>..HEAD`) and decide the bump from the
   Decision Gates above.
3. Set the new `info.productVersion` in `wails.json`.
4. Update `CHANGELOG.md`: promote `## [Unreleased]` to `## [X.Y.Z] — YYYY-MM-DD`,
   add a fresh empty `## [Unreleased]` above it, and write the entries in user
   language under the Keep a Changelog headings.
5. Append the release rationale to `docs/learning-log.md` with
   `node scripts/log-lesson.mjs "..."` — never by hand.
6. Commit on `dev` as `chore(release): bump to X.Y.Z`.

## Path A — Ship through CI (default)

7. Merge `dev` into `main` and push `main`.
8. Tag the merge commit on `main`: `git tag vX.Y.Z`. Confirm it landed where you
   think with `git tag --points-at HEAD`.
9. Push the tag. That, and only that, starts a release:
   `git push origin refs/tags/vX.Y.Z`.
10. Watch the run: `gh run list --workflow Release --limit 1`. On success the
    release is published — not drafted — at
    `https://github.com/Disble/autoreas-bridge/releases/tag/vX.Y.Z`.

`wails_tools.nsh` is regenerated **on the runner**, so a CI release does not
need it committed. It stays in the tree because a local build regenerates it
too; a diff there after a local build is expected noise, not a change to ship.

### What CI publishes

- `autoreas-bridge-X.Y.Z-windows-amd64-installer.exe`
- `autoreas-bridge-X.Y.Z-linux-amd64.tar.gz`
- `SHA256SUMS-windows-amd64.txt`, `SHA256SUMS-linux-amd64.txt`

**The portable Windows `.exe` is deliberately not published.** Bridge is an
installed application — it registers auto-start, lives in the system tray, and
expects a fixed install location. Wails still builds it and NSIS wraps it; it
just never becomes a release asset. It also carried blank Windows file
properties while the installer's were correct.

### The guards CI runs, and what each one catches

Every one of these exists because its failure mode is **silent** — a green build
that ships something broken. Do not remove one to make a run pass.

| Guard | Catches |
|---|---|
| tag vs `wails.json` | a tag that disagrees with the declared version |
| `INFO_PRODUCTVERSION` readback from the regenerated `wails_tools.nsh` | an installer built from a stale version define |
| `go list -f '{{.GoFiles}}' ./internal/tray` contains `systray_bindings_windows_cgo.go` | CGO off ⇒ the no-op tray vars stay, `Start()` still returns nil, the installer ships a dead tray |
| `go version -m` contains `bridgeVersion=X.Y.Z` | an unstamped build whose exported backups are labelled `dev` |
| CHANGELOG section exists for the tag | a release with empty notes |
| tagged commit is on `main` | a release cut from unmerged `dev` |

Linux builds with `-tags webkit2_41`: Wails' `gtk.go` pkg-configs
`webkit2gtk-4.0` unless that tag is set, and Ubuntu 24.04 ships only 4.1.

## Path B — Build locally

Use this to smoke-test before tagging, or when you need an installer without
publishing one.

7. Build with the stamp, deriving the import path instead of typing it:
   ```bash
   PKG=$(go list -f '{{.ImportPath}}' ./internal/desktop) || exit 1
   wails build -platform windows/amd64 -nsis -clean -ldflags "-X ${PKG}.bridgeVersion=X.Y.Z"
   ```
   The `-ldflags` is not optional. `internal/desktop/app_backup.go` defaults
   `bridgeVersion` to `"dev"`, and every exported backup bundle carries it into
   the user-visible import preview. **Go ignores a `-X` whose symbol does not
   exist — exit 0, no warning** — so a stale path ships `"dev"` in silence, which
   is why `go list` derives it. Read the stamp back before trusting it:
   ```bash
   go build -ldflags "-X ${PKG}.bridgeVersion=X.Y.Z" -o stamp-check.exe .
   go tool nm stamp-check.exe | rg -F "${PKG}.bridgeVersion.str"   # must print
   ```
   The `.str` symbol exists only when `-X` resolved. Neither obvious shortcut
   works: `go version -m` echoes back the `-ldflags` argument the build just
   passed (so it matches a binary that stamped nothing), and `go tool nm` on the
   shipped binary reports `no symbols` because wails appends `-w -s`. See
   ADR-018.
8. Verify the regenerated define matches:
   `rg 'INFO_PRODUCTVERSION "' build/windows/installer/wails_tools.nsh`.
9. Confirm the artifacts exist in `build/bin/`.
10. Verify the UI actually paints: `bun --cwd="frontend" run render:smoke`.
    **Launching the binary and seeing the process stay alive is NOT a smoke
    test** — 1.2.0 shipped a blank window behind a completely healthy Go startup
    log. See `.claude/skills/frontend-render-smoke/SKILL.md`.
    Then smoke-test what neither tests nor that check can reach (window/tray
    behavior, single-instance, installer flow). Report it as unverified if you
    cannot run it — never imply you did.

If a local build regenerated `wails_tools.nsh`, commit it with the version bump
rather than leaving the tree dirty.

## Known Issues, Not Regressions

- **The built `.exe` has blank Windows file properties** (ProductName,
  ProductVersion, FileVersion, CompanyName all empty). The **installer's** are
  correct — measured on 1.8.0: `Autoreas Bridge` / `1.8.0` / `autoreas-bridge`.
  Do not report this as caused by a bump. Note `-ldflags` does not fix it: that
  stamps a Go variable, not the Windows VERSIONINFO resource.
- **SmartScreen warns on every download**: *"isn't commonly downloaded"*. It is a
  reputation warning, not a detection. Both artifacts are unsigned
  (`Get-AuthenticodeSignature` ⇒ `NotSigned`), and wails' NSIS template ships the
  `signtool` hooks commented out at `build/windows/installer/project.nsi:70-71`.
  An unsigned file's reputation is tied to its exact hash, so **every new patch
  warns again** — this will not fade with downloads. Only an Authenticode
  certificate removes it. A self-signed certificate does not help, and sigstore
  signatures are not consumed by SmartScreen. The published `SHA256SUMS` files
  are the mitigation in place.

## Agent Notes

- SSH push may fail from an agent shell (`Permission denied (publickey)`) while
  `gh` is authenticated. Push without changing the user's config using a
  one-shot helper:
  `git -c credential.helper='!gh auth git-credential' push https://github.com/Disble/autoreas-bridge.git <branch>`
- A **draft** release 404s on `gh api repos/OWNER/REPO/releases/tags/<tag>`;
  drafts are not addressable by tag. List `repos/OWNER/REPO/releases` instead.
  Releases publish directly now, so this only comes up if `draft` is re-enabled.
- Local tags are not pushed by any other workflow. Before this skill's Path A,
  the repo's tags were local-only.

## Output Contract

Report: the old and new version, the regenerated `INFO_PRODUCTVERSION`, the
changelog section written, the branch the tag sits on, the workflow run result
with each guard's output, the published release URL with its asset list, and
explicitly which runtime checks were smoke-tested versus left unverified.

## References

- `wails.json` — the single source of the version.
- `.github/workflows/release.yml` — tag trigger, branch guard, publish step.
- `.github/workflows/build-windows.yml` / `build-linux.yml` — the two builds.
- `build/windows/info.json` — versioninfo template consuming `.Info.ProductVersion`.
- `docs/learning-log.md` — why entries, including the blank-exe-metadata finding.
- `AGENTS.md` → "Pre-commit Gate" — gate contents and timing.
